/**
 * Re-exported from @workspace/core — single source of truth for both web and TMA.
 */
export {
  useAppConfigStore,
  useAppConfigStoreActions,
  useAppConfigStoreInfo,
  useAppConfig,
} from '@workspace/core/stores';

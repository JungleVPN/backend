/**
 * Re-exported from @workspace/core — single source of truth for both web and TMA.
 */
export type { AppConfig, IAppConfigState as IState, IAppConfigActions as IActions } from '@workspace/core/stores';
